# 建仓说明（2026-09-22）

## 先说结论

`C:\FYP\data_repo` **还不是 git 仓库**（没有 `.git` 目录）。所以这不是「上传」，是「从零建仓」。
我已经做完了所有能在云端做的准备，**但 git 命令必须在你的电脑上跑**——我这边执行不了。

## 我做了什么

### ① 密钥全盘扫描 ✅ 通过

扫了 `scripts/` 下全部 33 个脚本（.py / .ps1 / .sh），用两套规则：

- 赋值形态：`api_key = "..."`、`token: "..."` 等
- 已知密钥指纹：`sk-`、`ghp_`、`AIza`、`AKIA`、`xox*`、JWT、32 位以上 hex、PEM 私钥块

**结果：零命中。** 仓库版脚本全部走环境变量：

```
collect_all.py:47   KEY = os.environ.get("OPENALEX_KEY", "")
collect_all.py:49   GH_TOKEN = os.environ.get("GITHUB_TOKEN", "")
collect_all.py:52   ORCID_SECRET = os.environ.get("ORCID_CLIENT_SECRET", "")
odp_patents_fetch.py:94  key = args.key or os.environ.get("ODP_API_KEY", "")
...（共 12 处，全部正确）
```

唯一一处告警是 `jobs_panel_fetch.py:33` 的 User-Agent 模板，误报。

⚠️ **注意**：扫的是**仓库版**。賈贇手上那份 `collect_all.py` 多 129 字节，**仍未核**。
他回传前不要把他那份覆盖进仓库。

### ② 其他敏感文件检查 ✅ 通过

| 文件 | 检查结果 |
|---|---|
| `.keys.env` | 真密钥，**已被 .gitignore 排除** |
| `.keys.env.example` | 空占位符，可以提交 |
| `raw/orcid_affiliated_researchers.csv` | 机构级聚合计数（11 行），**不含个人信息** |
| `raw/forward_panel/jobs_snapshot_*.csv` | 公开职位信息，无个人数据 |

### ③ 新版 .gitignore（文件已附）

旧版只有 4 行。新版增加：

- **个人层面底稿**：`openalex_author_inst_years.csv`（77 MB）、`openalex_author_quarters.csv`（42 MB）、
  `openalex_works_master.csv`（13 MB）、两个 ckpt 目录
  → 理由：提案 §7 承诺「结果不以可识别个人的形式发布」，推公开仓库与此冲突
- **更严的密钥规则**：`*.env`、`*.pem`、`*.key`（但保留 `.keys.env.example`）
- `_to_delete/`、`logs/*.log`、Excel 锁文件等

**复现性不受影响**：聚合层 csv 全部保留，`manifests/` 里有完整的来源 URL 与请求参数，
别人 clone 下来按 `docs/入库流程.md` 重跑采集即可得到相同底稿。

### ④ tasks/ 目录（文件已附）

```
tasks/
├── README.md       ← 留档规则
└── 任务台账.md      ← 总表，已填入当前全部 11 项在办 + 4 项已完成
```

台账已经把 J3 标成**逾期**、J6 标成**需改任务书**、L1 标成组长在办。

### ⑤ 建仓脚本（文件已附）

`建仓脚本_一次性.ps1`，7 步，**内置两道安全闸门**：

- 第 2 步：`.gitignore` 不是新版就拒跑
- 第 4 步：`git add` 之后检查暂存区，若出现 `.keys.env` 或任何大文件，**自动回滚并退出**
- 第 5 步：列出将提交的文件数与总体积，**停下来等你按 Enter**

## 你要做的（4 步）

1. 把附件里的 **`.gitignore`** 覆盖到 `C:\FYP\data_repo\.gitignore`
2. 把附件里的 **`tasks/`** 整个目录复制到 `C:\FYP\data_repo\tasks\`
3. 把 **`建仓脚本_一次性.ps1`** 放进 `C:\FYP\data_repo\`
4. PowerShell 里执行：

```powershell
cd C:\FYP\data_repo
.\建仓脚本_一次性.ps1
```

若没装 GitHub CLI，脚本末尾有不用 gh 的手动做法。

## 提交后的日常规矩

```powershell
# 每次改完东西
git add -A
git commit -m "说明改了什么"
git push
```

**派任务时**：任务单存进 `tasks/任务单/`，台账加一行，然后 commit。
**收回传时**：原样存进 `tasks/回传/`（不要改内容，R6），台账更新状态，然后 commit。

## 一件要提醒的事

仓库设为 **public**，意味着**从第一个 commit 起，全世界都能看到**，包括：

- `docs/` 里的全部内部讨论，含我们推翻自己结论的记录（`_作废_效度关结果`、`效度关结果_修订` 等）
- `tasks/` 里谁逾期了、谁的任务书写错了

这对学术诚信是**加分**——审阅者能看到完整的审计轨迹。但你可能想先跟两位组员说一声，
毕竟台账里写着「J3 逾期一周」。如果觉得不合适，改成 private 只要在脚本里把 `--public` 换成 `--private`。
