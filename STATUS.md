# 项目状态 · Phase 2

> **这份文件是项目的活状态，不是历史记录。**每轮结束更新，覆盖不追加。
> 数据本身的溯源看 `manifests/`；口径决定看 `docs/`；这里只答三个问题：
> **现在到哪了 · 卡在什么上 · 下一步做什么。**
>
> 最后更新：2026-09-12 ｜ 更新人：邱正阳（经 Claude 复算仓库文件后写入）

---

## 一、一句话现状

**采集阶段实质结束；效度关首轮已跑，两关都没过。**

2026-09-12 跑完步骤①，首版结论「关二通过、关一失败但可归咎于口径」
经独立复查后**不成立**——六条结论三条撤回、两条弱化。
真实状态是：**两关都没过，且指数在当前构造下无法做跨单元比较。**
详见 `docs/效度关结果_2026-09-12_修订.md`。

**当前第一优先不是补数据，是修指数构造**（缺失处理、参照集、累计/流量混用）。
根因已定位：**指数被用在了它承载不了的那一层**——六单元的 IDI 由不同指标集构成，
跨单元相减不是同质量之差。修法是三条口径规则（跨单元比较下沉到**指标层秩**、
指数只用于**单元内时序**、维度内时间属性统一），**不改研究类型、不需要新数据**。
完整执行方案见 `docs/执行方案_2026-09-12.md`（2026-09-14 组长同意，已进入 W1）。

⚠️ **W1 查出的新问题**：关一的三条已知格局里，**两条**的判定表述与提案 §3.1
所引文献支持的命题不是同一个命题（金融科技「双高」vs 文献的「板块最大」；
AI「人才陡升在前」vs 文献的「研究强/商业化人才弱」）。收缩后关一只剩**一条**
可判定的秩预期。建议关一降级为弱证据、效度重心移到关二并新增关三（判别效度）。
**冻结未完成前不得跑关一。** 见 `docs/效度关判定标准_冻结版v0草案_2026-09-14.md`。

**2026-09-12 阿里巴巴口径已拍板：方案③双版本**（正文剔除、附录原样、敏感性再加剔蚂蚁系），
三条专利序列与三条指数序列均已落盘带 manifest。
详见 `docs/阿里口径决定_方案三_2026-09-12.md`。
⚠️ **不带后缀的 `clean/index_{stock,flow}_by_quarter.csv` 从今天起等于剔除口径 `ali_grp`**。

---

## 二、仓库实测（2026-09-12 复算）

```
raw/        24 份原始件     manifest.py verify ✅ 哈希全绿（135 MB）
clean/      13 条清洁序列    每条带 .prov.json
scripts/    14 个           含 collect_openalex_master.py / works_build.py
docs/       9 份口径文档
```

### 六条季度序列的实际覆盖

| 序列 | 文件 | 行数 | 期数 | 单元 |
|---|---|---|---|---|
| 研究者存量（指标 1） | `clean/researchers_stock_by_quarter.csv` | 240 | 40 | **6** |
| 国际合著比例（指标 7） | `clean/intl_collab_by_quarter.csv` | 240 | 40 | **6** |
| 前10%高引占比（指标 4） | `clean/top10pct_by_quarter.csv` | 240 | 40 | **6** |
| 专利族（指标 8） | `raw/patents_families_by_quarter.csv` | 216 | 36 | 6 |
| 临床试验（指标 9 生医） | `raw/clinicaltrials_by_quarter.csv` | 80 | 40 | 2 |
| 牌照存量（指标 9 金融） | `clean/fintech_licenses_by_quarter.csv` | 40 | 40 | 1 |

### 指标 1 修正口径后的末期存量（2024Q4）

| 单元 | 本地署名口径 | 旧全作者口径 | v1 膨胀 |
|---|---|---|---|
| ai · hk | **5,633** | 17,456 | 3.10× |
| ai · sg | 5,382 | 15,358 | 2.85× |
| biomed · hk | **39,612** | 156,702 | 3.96× |
| biomed · sg | 34,972 | 145,234 | 4.15× |
| fintech · hk | **1,158** | 2,775 | 2.40× |
| fintech · sg | 789 | 2,116 | 2.68× |

**一个对效度关有利的信号**：金融科技香港 1,158 vs 新加坡 789，**香港高 47%**，
支持「金融科技双高」的前半——这是修正口径后才看得见的。

### ⚠️ 一个未解释的差异

biomed/hk 论文数从 70,062（2026-08-26，`meta.count` 40 次季度请求）
变成 85,760（2026-09-11，cursor 逐篇枚举），**+22.4%**，而其他五个单元只涨 0.02–3.7%。

已排除：重复计数（work_id 全唯一）、索引回填（增量在十年均匀分布、机构分布正常）、
filter 差异（两边源码一致）、采集失败（无归零季度）。
剩余最可能：`meta.count` 与可枚举条数在大结果集上不等。
**处置**：新数据（逐篇枚举）作主口径，旧文件降为交叉验证参照。
**验证只需一条命令**——对 biomed/hk 2015Q1 同时取 meta.count 与枚举条数比对。

---

## 三、卡点（按「不解决就出不了东西」排序）

### ① 季度指数由哪几个维度合成 —— 待组会拍板

七个维度里有两个的指标在附录 A 就是年度：**TAI 流动**（指标 6）、**IDI 资本**（指标 12）。
而 §4.2 写「年度指标绝不内插为季度」，所以它们进不了季度指数。

**要确认的一句话：**
> 季度指数由该指数下「有季度指标的维度」等权合成；无季度指标的维度只进年度剖面。

- **认** → TAI 季度＝规模＋质量＋网络，IDI 季度＝创新＋商业。补下面三件事即可跑效度关
- **不认** → 整个季度指数设计要重新讨论

⚠️ 这是从 §4.2 **推导**的，提案没明写「维度可以缺席」。答辩会被问，需有人认这个说法。

### ② TAI 规模维完全空 —— 唯一的数据拦路虎

规模维靠指标 1（研究者存量）与指标 2（活跃开发者），**两个都无数据**。
TAI 的定义是「从供给端测量人」，规模维＝有多少人。
效度关一要检验「AI 人才陡升在前」，检验的正是规模维。

**2026-09-10 更新 —— 指标 1 采过一轮，但口径错了，要重采。**
賈贇 09-07 跑完的 v1 底稿机械上完全干净（哈希对上、132 万行无重复、
6 单元 × 40 期无缺格、clean 240 行组长侧独立复算逐值相符），
但 `collect_authors.py` v1 把整篇论文的**全部作者**都记成了本地研究者——
OpenAlex 的 `institutions.country_code:hk` 是**论文级**筛选，不是作者级。
实测：biomed 港星作者集重叠 **21.4%**（60,514 人同时算港又算星），
biomed/hk 三年存量末期 **156,510 人**，高出全港研发人口一个数量级。
脚本已于 09-10 打补丁（底稿多存 `is_local` 列，主口径与全作者口径同时留存，
调用次数与耗时不变），重采指引见 `../给賈贇_指标1重采_2026-09-10.md`，
完整诊断见 `docs/指标1_v1底稿作废与重采说明.md`。
v1 原包存档于 `raw/_archive/指标1底稿v1_全作者口径_賈贇_20260907.zip`，不入 raw/clean。

**另两个已知的窗口限制（重采也解决不了，方法章要写）：**
- 三年滚动窗口的**前 11 期不完整**（2015Q1–2017Q3），标 `window_full=False` 不填补。
  先行检验只能用 2017Q4 起 **29 期**；与指标 8 的 36 期取交集后**实际约 25 期（2017Q4–2023Q4）**。
- `new_authors` 有**左截断偏误**：2015Q1 之前无历史，早期的"新人"其实是老作者首次进入样本。
  新版脚本加了 `new_authors_usable` 列，**2018Q1 起才可用**。

### ③ 指标 7 分子缺 fintech 两个单元 —— ✅ 2026-09-11 已由统一采集包解决

**根因已定位**：`scripts/collect_all.py` 的 `mod_papers()` 里，
`if flt:`（AI/biomed 学科白名单分支）有 `rows_i.append(...)`，
`else:`（fintech 关键词分支）**只有 `rows_n.append`，没有 `rows_i.append`**——分子从未被写入。
修法：在 else 分支补一次带 `countries_distinct_count:>1` 的调用。增量 2 城市 × 40 季 = 80 次。

**但这条路已经不用走了。** 统一采集包 v2（`scripts/collect_openalex_master.py`）
在拉指标 1 的同一遍历里，数每篇论文 `authorships[].institutions[].country_code` 的去重个数，
**零额外调用**就得到六个单元完整的国际合著比例（`clean/intl_collab_by_quarter.csv`）。
`collect_all.py papers` 的补丁保留不动，作为**独立的交叉验证路径**——
`works_build.py` 会自动把两边的分母逐格对照并报告一致格数。

---

## 四、下一步：项目已越过转折点，从「补数据」进入「跑分析」

> **2026-09-12 重写**：采集阶段实质结束。六个季度指标中五个已有数据，
> **步骤①效度关现在就能跑**——不必等指标 2。

### 为什么现在就要跑效度关，而不是继续补数据

提案写死「指数不过效度关，后续分析不展开」。这句话的另一面是：
**效度关的结果决定后面十周做什么**。现在是第 5–8 周，若关一不过，还有时间调口径；
拖到第 12 周才跑，发现问题就没救了。所以效度关是**当前唯一的关键路径**。

| # | 做什么 | 成本 | 为什么是这个顺序 |
|---|---|---|---|
> **2026-09-12 再次重排**：指标 11 与首轮效度关已做完，但结果不成立。
> 下面是复查后的顺序——**前三件都是修构造，不是补数据**。

| # | 做什么 | 成本 | 为什么是这个顺序 |
|---|---|---|---|
| ~~1~~ | ~~拍板阿里巴巴三选一~~ | ✅ **2026-09-12 已完成** | 拍板方案③；三条口径序列已落盘。fintech 的 Δz 从 +0.413 变为 **−0.027（归零，非翻转）** |
| **1'** | **把 geo_rule 一致地用到指标 11** | 人工半天 | 18A/18C 判定表已建好骨架（`config/listings_geo_review.csv`，70 家全空）。**不填＝指标 8 与指标 11 的「香港」不是同一个香港** |
| **2** | **重写指数的缺失处理** | 半天 | 「维度内取可得均值」是一种插补而非中性。跨单元比较改为只在**共同指标集**上做 |
| **3** | **冻结三个已知格局的出处与判定阈值** | 一小时 | 看到结果后再改口径＝取消这道关。必须先写死再重跑 |
| **4** | 关二重做：补 C&SD 高等教育界别序列 | 采集半天 | 现分母（商业界别）不含大学医院，生医比值 270% 即证据；且须报去趋势／差分相关 |
| **5** | 同一维度内统一时间属性（累计 vs 流量） | 半天 | 牌照与上市是累计（lag1 ≈ 0.95），专利是流量，等权平均时实际权重由方差决定 |
| **6** | 低通检验改在斜率与 cross-correlation 上做 | 半天 | 看窗口均值的符号排除不了低通 |
| **7** | 招聘面板开跑（时效性） | 每周一次 | **流量型数据，每推迟一周永久少一周** |
| **8** | SFC Table C4／C6 历史深度确认 | 十分钟 | 决定 fintech 规模维能否换源 |

⚠️ **跑效度关时必须同时出两个版本**：三年滚动**存量**（进指数）与
**流量**（`new_authors`，2018Q1 起）。低通问题导师未表态，两个版本一起看才知道
「AI 人才陡升在前」是真的测不出，还是被 12 期窗口摊平了。

### 当前六个季度指标

| 指标 | 状态 |
|---|---|
| 01 研究者存量 | ✅ 已入库（本地署名口径，240 行） |
| 07 国际合著比例 | ✅ 已入库（6/6 单元，240 行） |
| 08 企业专利族数 | ✅ 已完成（36 期） |
| 09 牌照/临床试验 | ⚠️ 3/6（AI 无源属结构性；缺星洲牌照） |
| 11 18A/18C 上市 | ⏳ 本地待切（半小时） |
| 02 活跃开发者 | ❌ 未启动 —— 唯一实质缺口 |

---


## 五、明确**不做**的（现在做＝无用功）

- **指标 3、4、5、6、10、12** —— 六个全是年度指标，按 §4.2 只进年度剖面（步骤②），**不阻塞步骤①③**。效度关不过，步骤②③根本不展开
- **指标 2（GitHub 活跃开发者）** —— 与指标 1 同属规模维，有一个维度就成立。指标 1 口径/白名单/数据源全部现成；指标 2 还要先解决怎么枚举驻港账户（Search API 1000 条上限、location 是自填自由文本，`HK`／`香港`／`Hong Kong SAR` 都要处理）。留作规模维第二个指标
- **继续精修专利** —— 指标 8 已收口

---

## 五点五、⚡ 导师 consult 决定（2026-09-11，最高优先级）

以下三条由导师当面给出，**优先级高于本文件其余所有推导**：

| # | 导师原话 | 直接后果 |
|---|---|---|
| 1 | 数据不完美时**应求合理而不必一致**，但要在报告中说明 | 六个单元**不必强求同一把尺子**。解开了「金融科技学术痕迹稀薄」的死结——但引入新问题：口径不一致时，跨产业的 z-score 合成还能不能做数值比较（见下） |
| 2 | 金融科技人数：**论文少可以换别的指标（资格证书等）再说明** | 指标 1 对 fintech 换源。⚠️ 约束：提案 §4.5 要求「新加坡侧仅使用双城对称的数据源」——**换源前必须先确认新加坡有对称源**，否则港星对比在这个指标上会断 |
| 3 | 流动维：**不影响对人才的定义、大研究方向的话可删** | TAI 由四维降为**三维（规模/质量/网络）**；指标 6 三通道全部不做。⚠️ 连带：RQ1 明写「规模／质量／流动／网络」要改；§2.2 引 Fedyk & Hodson 论证「存量与流动携带不同信息」要相应调整；政策讨论（高才通前后流入构成）改以 C&SD 与入境处数据作纯描述性证据，不进指数 |

**✅ 2026-09-12 已落到提案**：`C:\FYP\FYP_Proposal_修订版v4.2.docx`（17 页，XSD 校验通过）。十一处改动：

1. RQ1「规模／质量／流动／网络」→「规模／质量／网络」
2. §2.4 构念界定「四个维度」→「三个维度」
3. 表 3 数据源归属「TAI 全部四维」→「TAI 全部三维」
4. §4.2 流动维三通道段落 → 改写为「关于流动维的取舍（v4.2）」，写明学术通道结构性不可识别
5. §4.2 新增两条口径原则：求合理而不求形式一致 ＋ 金融科技改用资格证书类指标
6. §4.2 新增可比性代价声明：**跨产业指数数值不作直接高低比较，只比剖面形状与各自时序；产业内时序与港星同产业对比不受影响**
7. §4.6 局限补：学术痕迹对产业界人才系统性缺失，且缺失程度三产业间不一致
8. 附录 A 表 6 删除「人才净流入率（三通道）」整行 → **12 指标变 11 指标**
9. 附录 D 新增「v4.1 → v4.2 增量」三列表（沿用 v4→v4.1 的同一格式）
10. 附录 D 数据规模更新为 24 份原始件、13 条清洁序列
11. 封面版本号 v4.1 → v4.2

✅ **2026-09-12 已核验**（详见 `docs/金融科技替代源核验_2026-09-12.md`）：

**结论：持牌人员层面港星不对称，且补不回来。**

- **香港侧比预期好**：SFC 统计表 C1–C6 季度更新、Excel 可下载、按受规管活动细分，**C6 的虚拟资产（AMLO）持牌人是金融科技最直接的官方代理**，C4 给的是人数
- **新加坡侧缺口**：MAS Representative Register 是**在线名录查询，不是统计时间序列**。要做序列只能从今天起自建快照——**历史无法回溯**，对 2015–2024 的研究等于没有
- **看似对称的退路更差**：两地统计局的「金融及保险业总就业」确实对称，但装的是柜员、保险代理、后台——**与目标构念的距离比论文还远**。对称但无效，不优于不对称但有效

**采纳方案**（依据导师第一条「求合理而不必一致」）：金融科技规模维用 SFC 持牌人数，**只做香港时序**；该产业的港星对比退到质量维与网络维（两维仍是 OpenAlex 双城同源）。

⚠️ **一项待确认**：SFC 页面只说「更新至 2026 Q2」，**未说最早回溯到哪年**。下载 Excel 确认能否覆盖 2015Q1 是动手前第一件事；若覆盖不足则退回论文口径或缩短金融科技窗口。

---

## 六、组会待拍板

> **2026-09-11 组长已拍板两项**（邱正阳）：
> - `INSTITUTION_SCOPE = country`（本轮就按这个跑，不再等）
> - 低通问题选 **方案③：存量与流量双序列**（详见下）
>
> 以下两项保留为记录与依据，不再是待决项。

**✅ 已拍板 —— 三年滚动存量的低通特性（2026-09-11 提出，同日拍板③）**

这一条比口径 bug 更深，**重采也解决不了，且现在就能拍、不必等数据**。

指标 1 是「近三年滚动窗口内的唯一作者数」，即对底层人才流量做 **12 期矩形窗卷积**。
数学后果两条：**相位延迟约 (12−1)/2 = 5.5 期**；**高频成分被强烈衰减**。
由此派生三个直接打击研究设计的问题：

1. **效度关一可能因指标构造而失败** —— 关一要检验「AI 人才陡升在前」。
   任何一次季度级陡升，在 12 期窗口下都被摊平成缓坡。
   检验不出来时，我们会误判成「数据不支持假说」，其实是「指标测不出陡升」。
2. **步骤③的预注册滞后会系统性落空** —— 预注册的「人才→创新 4 季」是对**流量**的预期。
   用存量去测，峰值滞后会右移约 5.5 期，真信号落在 9–10 季而不是 4 季，
   于是被判成不显著。**预注册窗口与指标构造不匹配。**
3. **置换检验的 p 值不可信** —— 相邻两期共享 12 个季度里的 11 个，
   人工自相关极强；同比增长率去趋势也不能消除。置换检验的原假设分布被破坏。

**三选一 → 已选 ③：**
- ① 缩短窗口（如 4 期）——相位延迟降到 1.5 期，但附录 A 明写三年，要改方法章
- ② 只改预注册滞后窗——承认存量口径，把「4 季」放宽到「4–12 季」，但 ③ 的 p 值问题仍在
- ③ ✅**【已采纳】存量与流量双序列**：三年滚动存量进指数合成（维持附录 A），
  另出 `new_authors`（季度首次出现作者数）作为**流量序列专用于步骤③的先行检验**，
  方法章显式声明两者关系。
  ⚠️ 注意这与此前自我推翻的「选项 C」**不是同一件事**：
  那次的问题是把流量代理**作为独立指标加进指数**、与规模维构造共线；
  这里流量序列**不进指数合成**，只在检验环节用，不产生共线。
  代价：`new_authors` 有左截断，**2018Q1 起才可用**（脚本已加 `new_authors_usable` 列）。

**✅ 已拍板 —— INSTITUTION_SCOPE = `country`**

`country`（作者机构国别 = hk/sg，与指标 7 分母同源）vs
`roster`（附录 A 的八大＋公营研发机构 ROR 名单，定义准但 InnoHK 无 ROR 采不到）。
**2026-09-11 组长定：本轮用 `country`。** roster 留作稳健性口径，若要做则另起一轮，不阻塞本轮。

**✅ 已拍板 —— 阿里巴巴口径 = 方案③ 双版本（2026-09-12 邱正陽）**

窗内 company 口径下，阿里系占 **fintech/hk 86.2%、ai/hk 55.6%**；
剔除后 fintech/hk 从 22.9 件/季掉到 3.2 件/季（只剩 13.8%）。

落地：`asis`（附录）／`ali_grp`（**正文主口径**）／`ali_ant_grp`（敏感性，再加剔蚂蚁系）
三条序列，见 `raw/patents_families_by_quarter_company_*.csv` 与
`clean/index_*_{asis,ali_grp,ali_ant_grp}_by_quarter.csv`。
配置在 `config/patents_assignee_geo_exclude.csv`，**两城同剔**。

⚠️ 三点必须一起记住：
1. **必须有第三档**。只剔阿里、留下 `ALIPAY LABS SINGAPORE`（占 fintech/sg 11.3%），
   等于剔掉香港巨头、留下新加坡巨头，方向恰好对我们有利。
2. **反论成立**：阿里的专利是真实发生在香港的申请。剔除 = 采取「产业基础」而非
   「产业产出」视角，是立场选择而非数据清洗，**必须写进方法章**。
3. **「阿里在港是否有实质研发」这个事实判定仍未做**
   （`substantive_rd_verdict` 列全空）。方案③的设计就是不依赖它。

全文见 `docs/阿里口径决定_方案三_2026-09-12.md`。

**指标 3 份额口径**：§3.2 说**论文**、附录 A 表 6 说**研究者**，提案自相矛盾。
已采的 `openalex_share_denominators_by_year.csv` 是论文数。
**选论文＝零返工，选研究者＝重采一轮。**

---

## 七、⚠️ 未入库项（复现风险）

> **2026-09-11 已解决**：賈贇欠的三样（`config/patents_assignee_sector.csv`、
> 填好的 `patents_assignee_sector_review.xlsx`、三条 sector 序列）**全部回仓**，
> 缺失的 manifest 已补。指标 8 现在可从仓库完整复现
> （`patents_geo_variants.py` 的 `asis` 变体已校验与既有 company 序列字节一致）。

**根因仍未解决**：賈贇没有仓库访问权，只有打包发过去的文件；他在解压目录里跑，
产物就留在那里。本轮已第三次发生（此前：填好的 xlsx 留在任务包、诊断 SQL 只放进组长仓库）。
**长期解法是给他仓库访问权**，而不是每次在说明里叮嘱。

### 仍未解决的复现风险

- ~~四个 OpenAlex 底稿缺 `request_log`~~ **✅ 2026-09-12 已修**。
  `manifest.py verify` 现在**全部通过**（27 个文件，哈希全绿）。
  ⚠️ 顺带纠正一条曾经的判断：这**不是**「跑一下 `rebuild_request_urls.py` 就好」——
  该脚本的 `BUILDERS` 表写在统一采集包之前，根本没有这四个文件的条目。
  已补两个 builder（`build_master_unit`、`build_topjournal_denoms`）。
  重建的 URL 是**按采集代码确定性重建**的、不是采集当时记录的（R1 诚实性声明已在脚本里），
  可信度由 R4 点开 URL 核对计数来验证。
- **`qa_check.py` 的 schema 表已过时，红字里有 34 条是误报**：
  它按文件名后缀套 schema，`_by_quarter.csv` 一律要求有 `count` 列，
  而我们的季度表用的是 `unique_authors` / `intl_share` / `cumulative` 等实名列
  （13 条「缺列 ['count']」全是这个），`_series.csv` 宽表则把中文机构名当成「非数值单元格」（19 条）。
  **看到 qa_check 红字不要当成数据问题**，先分辨是不是这两类。
  2026-09-12 已加 `mixed` 规则型（文本标识列 + 指定数值列），新表已通过；
  旧表的 schema 待逐个对齐。
- `config/listings_geo_review.csv`、`config/patents_assignee_geo_exclude.csv`
  的判定列全空——不是缺失，是**待人工核**（R1：AI 不臆断事实）。

---

## 八、已证伪的判断（别再重复）

新接手者请注意，以下说法**已被验证为错**：

| 曾经的说法 | 实际 |
|---|---|
| 「指标 1 采集量大，要先估排期」 | 约 **930 次调用、半天**。这个误判让最关键的一件事被推迟数轮 |
| 「指标 1 采完了，规模维有数了」 | v1 口径错（论文级 filter 当成作者级用），**绝对水平、相对水平、增长率三者全部不可用**，2026-09-10 已作废重采 |
| 「效度关二通过（r=+0.759）」 | 两条序列都是单调上升。**去趋势后 r=+0.161（p=0.69）、一阶差分 r=−0.002**。原始相关几乎全是共同趋势 |
| 「stock 与 flow 结论一致，所以低通不是失败原因」 | 两版本单元均值相关 **+0.9959**，一致是构造必然，**不能排除任何东西**。斜率层两版本差 0.33–1.66 倍，低通怀疑未被检验 |
| 「网络维不受产业基数影响（corr +0.060）」 | 6 个点实际只有 3 个独立取值。**聚合到产业层 corr = +0.945**，网络维是跨产业可比性第二差的维度 |
| 「港星对比重现了两个已知格局」 | ΔIDI 是**跨维度构成相减**，敏感性区间跨度 4 倍；fintech 的正号依赖未决的阿里断点（剔除后归零）；且「产业双高」被替换成了「港胜于星」——**移动球门** |
| 「TAI 一定是四个维度」 | 2026-09-11 导师：流动维**可删**。TAI 降为规模/质量/网络三维 |
| 「六个单元必须用同一个指标口径」 | 2026-09-11 导师：**求合理而不必一致**，说明即可。金融科技可换资格证书类指标 |
| 「重采完指标 1，规模维就能过效度关了」 | 重采只解决「作者归属」。还剩：滚动窗口低通（第六节第一项）、可用期数仅约 25、指标 2 仍空、只覆盖发论文的人、biomed 白名单＝整个 Medicine、OpenAlex 中文姓名消歧差。**有数 ≠ 能用** |
| 「`--dry` 是安全的空跑」 | `collect_authors.py` 原来用 `os.environ["DRY"]="1"`，而 `collect_all.DRY` 是 import 时定好的模块级变量、根本不读环境变量——**`--dry` 会真的发请求**。2026-09-11 已修 |
| 「Google Patents 可用 `FP=(family_id)` 查专利族」 | **该操作符不存在**。官方语法只有 `assignee:` `inventor:` `before:` `after:` `country:` `cpc:` 等，**无任何按族 ID 检索的入口**。DOCDB family_id 是 EPO 内部编号，前端不接受 |
| 「剔除阿里后 fintech 的 Δz 翻转成 −0.312（香港显著低）」 | **数值错、方向也过强**。旧算法只匹配 `ALIBABA` 字样（漏斑马、菜鸟、全部蚂蚁系），且用各单元自身窗口（错配）。正确口径（集团级名单＋共同季度交集）是 **−0.027**——不是翻转，是**归零、方向不可辨** |
| 「fintech/sg 没有阿里系申请人，所以只剔香港侧就行」 | `ALIPAY LABS SINGAPORE` 等蚂蚁系三家占 fintech/sg **11.3%**（61 族）。旧筛查只匹配 `ALIBABA` 字样所以看不见。**只剔阿里＝剔掉香港巨头、留下新加坡巨头**，必须报 `ali_ant_grp` 敏感性档 |
| 「geo_rule 是阿里这一个特例的处理」 | 同一条规则同样适用于指标 11：窗内 70 家 18A/18C 里 **67 家是 18A 生物科技**，板块以内地企业为主体。不一致执行＝正文里「香港商业化维」和「香港创新维」不是同一个「香港」 |
| 「阿里的专利转去了港星之外的主体」 | **未验证的推测**。复核查到的是阿里系几个主体在 2020 年前后**集体停止新申请**，原以为的接收方 ADVANCED NEW TECHNOLOGIES 自己也停了。**去向未确认** |
| 「指标 3 用论文还是研究者，是组会的偏好选择」 | **可行性已经锁死了答案**：研究者口径要对亚太（含中国大陆）跑一遍全量 authorships 遍历，两万次以上调用、数天、数十 GB，预算内不可行。只能用论文口径，分母已入库 |
| 「TAI 质量维有数了（大学专利 6/6）」 | 大学专利**在附录 A 表 6 里没有指标编号**，它来自 §4.3 的专利三分归属规则。质量维的正牌指标是 4、5（都是 OpenAlex）。把大学专利当质量维季度指标是**超出附录 A 的新设定，须在方法章声明** |
| 「跨产业家族唯一分配需组会拍板」 | 提案 §4.3 **已写死**：「跨界实体按主营唯一分配并辅以『双计入』敏感性检验」。只剩「主营」如何操作化这一技术细节 |
| 「public_rd 占 10.2%」 | 那是只算高置信度一档的数。全量：按产业×城市×家族记录 **11.2%**（賈贇人工归类后），按申请人家族次 10.7%。三种口径都过 10% 线，结论不变 |
| 「三族 CPC 与产业标签不符」 | 数据无误。是 **Google Patents 的 Classifications 栏默认折叠**，复核者只看到前几个码。详见 `docs/CPC标签口径说明.md` |

---

## 九、相关文档索引

| 文件 | 内容 |
|---|---|
| `docs/效度关判定标准_冻结版v0草案_2026-09-14.md` | **⭐⭐ 关一的判定标准草案。核心发现：三条已知格局里两条的判定表述与所引文献不是同一命题；关一收缩后只剩一条可判定秩预期。含 5 项冻结前必清的人工待办** |
| `docs/W1任务指引_2026-09-14.md` | W1 三项的状态与操作命令；招聘面板降级理由、C&SD 已在本地解决 |
| `docs/执行方案_2026-09-12.md` | **⭐⭐ 从今天到 11.6 的完整执行方案：三条口径规则 R-A/R-B/R-C、效度关重新定义与防移动球门程序、8 周倒排表与分工、两个新查出的构念错配、prof 会问的三个问题** |
| `docs/阿里口径决定_方案三_2026-09-12.md` | **⭐ 方案③的完整决定、三档口径与剔除名单、落差表、必须写进方法章的反论、geo_rule 一致性清单** |
| `docs/阿里断点三方案实测_2026-09-12.md` | 拍板前的三方案实测（**数值已被上一条取代**，方向性结论仍成立） |
| `docs/效度关结果_2026-09-12_修订.md` | **首轮效度关的真实结论（两关都没过）、六条改判、唯一保留的实质发现** |
| `docs/金融科技替代源核验_2026-09-12.md` | **SFC／MAS 对称性核验的三层结果、为何不退到行业就业口径、待确认的历史深度** |
| `docs/统一采集包v2_规划.md` | **一次遍历五个指标的设计、四个口径的覆盖率、三个拿不到的及原因** |
| `docs/指标1_v1底稿作废与重采说明.md` | v1 作者归属缺陷的三条证据、修补内容、v1 原包的处置 |
| `docs/CPC标签口径说明.md` | CPC 标签的现行口径、R4 诊断的三项结论、不加 inventive 过滤的三条理由 |
| `docs/申请人三分归类速查.md` | company/university/public_rd 判断标准与四类陷阱 |
| `docs/附录B_CPC白名单速查.md` | 三产业 CPC 白名单、条件码 H04L9、明示排除项 |
| `docs/R4复核操作指南.md` | R4 与 `manifest.py verify` 的区别、抽样方法 |
| `docs/BigQuery操作手册.md` | GCP 沙盒与专利查询 |
| `docs/Phase2采集进度.html` | 六单元 × 五序列覆盖矩阵（浏览器打开）|
| `docs/效度关前的最短路径.html` | 七维度拆解 + 三步计划（浏览器打开）|
