# 任务单 · 賈贇｜W3（9/22 起）

> **先说三件我欠你的，都是我的错，不是你的。**
>
> **① 脚本没发给你** —— 我在上一份任务单里写「`ct_phase_probe.py` 已发给你，在 `scripts\` 下」，
> 但你根本没有仓库访问权，我从来没真的发过。这是我第二次犯同样的错（上次 `oa_count_probe.py` 也是）。
>
> **② diff_safe 少给了对照物** —— 你说得完全对：它要比对「你本地版」和「仓库版」两个文件，
> 而仓库版在我这儿，你手上只有自己那份。给了工具没给对照物，用不了是必然的。
>
> **③ 这两个问题现在一次性解决了** —— 仓库已经建好并公开：
>
> ## 👉 https://github.com/Qiuu2/FYP
>
> 以后所有脚本、配置、文档你都能直接拉，不用再等我发压缩包。

---

## 零、先把仓库拉下来（5 分钟，一次性）

装了 git 的话：

```powershell
cd C:\    # 或任何你想放的地方，别放在现有 FYP 文件夹里面
git clone https://github.com/Qiuu2/FYP.git
cd FYP
```

没装 git 也行 —— 网页上点绿色 **Code** 按钮 → **Download ZIP**，解压即可。
（但装了 git 以后更新只要 `git pull`，强烈建议装：https://git-scm.com/download/win）

拉下来之后你就有了：

| 目录 | 内容 |
|---|---|
| `scripts/` | **全部脚本**，含 `ct_phase_probe.py`、`oa_count_probe.py`、`diff_safe.py`、`collect_all.py` 仓库版 |
| `config/` | 边界规则、公司名单、申请人归类表 |
| `docs/` | 所有口径决定与分析文档 |
| `tasks/` | 任务单与回传留档（就是你现在看的这份） |
| `raw/` `clean/` | 数据（超大的个人层面底稿已排除，见 `.gitignore`） |

⚠️ **密钥不在仓库里**（也不该在）。你本地的 `.keys.env` 继续用你自己那份。

---

## 一、J1 本周已收 ✅ 但有一处要你确认

manifest 写得很规范 —— 真实采集人、SHA-256、source_url、UTC 时间戳都齐全。

**但有一个问题**：本周快照与上周（9/15）的 **SHA-256 完全相同**：

```
bd4da69828cdca9eca0721320f48beee4c68c801c494b7b301ee7350c5344e70
```

11 行内容一字不差。

**两种可能，你确认一下是哪种**：

- **(a)** Animoca 的职位板这两周确实没变动 → 完全正常，据实记录即可
- **(b)** 脚本读了缓存，或者不小心跑/传了上周那份文件

判断方法：直接打开 https://api.lever.co/v0/postings/animocabrands?mode=json
数一下现在有几个岗位。如果就是 11 个且标题一致，那就是 (a)。

**回复一句话就行**，不用写文档。

---

## 二、J4 —— 脚本现在你有了

拉下仓库后：

```powershell
cd <你clone的位置>\FYP
python scripts\ct_phase_probe.py
```

它会把 2024Q1 香港的全部临床试验记录拉回来，统计 `phases` 字段的真实取值组合，
直接打印「多值组合有多少」「无该字段的有多少」。

**要解决的问题**：你测出总数 109，但分项只加到 95（含你发现的 `EARLY_PHASE1`），差 14。

两种可能，靠逐条 count 永远分不出来：

- **(a)** `phases` 是多值字段（如 `["PHASE1","PHASE2"]`），而 `AREA[Phase]PHASE1` 只匹配单值
- **(b)** 有些试验根本没有 `phases` 字段

⚠️ 线索指向 (a)：如果是「包含」匹配，分项和应该**大于**总数；现在是小于，说明组合期别两边都没数到。

**产物**：整段输出粘回来，不用自己下结论。

---

## 三、diff_safe —— 现在有对照物了

仓库版 `collect_all.py` 是 **32,908 字节**。你本地那份是 33,037 字节，多 129 字节。

拉下仓库后：

```powershell
cd <你clone的位置>\FYP
python scripts\diff_safe.py --old scripts\collect_all.py --new "C:\你本地的路径\collect_all.py"
```

（如果脚本参数名不是 `--old/--new`，直接 `python scripts\diff_safe.py --help` 看一下）

它会把疑似密钥的长字符串遮蔽掉再输出 diff，**所以结果可以直接发回来，不会泄露**。

**为什么非查不可**：仓库现在是**公开的**。如果你那份写死了 API key 而我们直接合并进去，
密钥就进了 git 历史 —— 删文件也没用，得重写整个历史。所以 **J4 全量重采之前必须先过这一关**。

**产物**：diff_safe 的输出粘回来。

---

## 四、J1 要不要继续跑 —— 这个我先说我的判断

`config/jobs_panel_companies.csv` 里有 12 家公司，但**只有 1 家（Animoca）是 `confirmed`**，
其余 11 家全是 `pending_confirm`，ATS token 未知。

也就是说这个面板现在只覆盖一家公司。而且更根本的问题：

**招聘数据是 2026 年的，我们的研究窗口是 2015–2024。** 它对本轮分析的边际价值接近于零。

**我的建议：J1 暂停，不再每周跑。** 理由是把你的时间放到真正卡住结论的事情上（见下）。
如果以后要做「前瞻面板」的延伸分析再重启，届时已积累的快照仍然有用。

**这个需要组长拍板，你先别停，等他确认。**

---

## 五、J6 —— 我写错了，作废重来

原任务单让你「对每件专利去 Google Patents 打开，记录发明人地址」。

**我实测过了，这个做法行不通**：

- Google Patents 的专利页**只显示发明人姓名，不显示地址**
- 底稿 `raw/patents_families_raw.csv` 里只有 `family_id`（DOCDB 家族号），没有专利号
- Google Patents 的搜索页禁止程序抓取，没法用家族号反查专利号

**这是我写任务单时没核实，不是你的问题。** 20 件手工查下来也拿不到地址。

**替代方案待定** —— 可能改用 Espacenet（欧洲专利局，按 INPADOC 家族号检索且著录项含地址）
或 PatentsView API。这需要写代码，我来做，不占你工时。

**你不用做任何事**，J6 作废。

---

## 六、本周汇总

| # | 任务 | 工时 | 产物 | 状态 |
|---|---|---|---|---|
| **0** | clone 仓库 | 5 分钟 | — | **先做这个** |
| **1** | J1 同哈希确认 | 2 分钟 | 回一句话 | 新 |
| **2** | diff_safe 查 collect_all | 10 分钟 | 输出粘回 | **J4 的前置** |
| **3** | J4-1 phase 差额诊断 | 20 分钟 | 输出粘回 | 脚本现在你有了 |
| **4** | J4-2 phase 全量重采 | 半天 | csv + manifest | 等前三项 |
| ~~J6~~ | ~~发明人地址抽样~~ | — | — | **作废（我的错）** |
| J3 | 计数悬案探针 | 10 分钟 | 输出粘回 | 仍未做，脚本在 `scripts\oa_count_probe.py` |

**顺序**：0 → 1 → 2 → 3 → 4。第 2 步没过，第 4 步不要开始。

---

## 七、J4-2 的一个提前告知

2024Q1 香港 109 个试验里 **73 个是 NA**，有明确期别的只有 22 个，PHASE3 只有 12 个。
整个窗口下来 PHASE3 大概每季 10 上下 —— **这个量级噪声可能盖过信号**。

不妨碍先把数拿到，但拿回来之后要先看序列形状再决定能不能用。
**你不用为这个结果负责**，把数采准就行。

---

## 八、以后怎么交东西

仓库建好了，但**你暂时还是发给组长**（写权限还没配）。

如果想直接提交，跟组长说一声加你为 collaborator，之后就是：

```powershell
git pull                      # 每次开工前先拉最新
# ...干活...
git add -A
git commit -m "J4-1 phase 差额诊断结果"
git push
```

**回传老规矩**：每完成一项立刻发回，csv 和 manifest 一起发，别攒到最后。
