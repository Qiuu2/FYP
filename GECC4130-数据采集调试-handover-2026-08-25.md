# 项目交接：GECC4130 FYP · Phase 2数据采集调试 — 2026-08-25
> 版本 v1 ｜ 下次更新触发条件：test_patentsview.py 实测结果拿到手 / patentsview 根因确认并修复后

## 0. 给新Claude的一句话
接手的是GECC4130 Senior Seminar FYP项目（人才基础指数TAI/产业发展指数IDI，港星比较）Phase 2采集脚本的调试：组员賈贇跑出的raw.zip里发现3类数据异常，2个代码bug已经在`collect_all.py`里定位并修复，剩1个（PatentsView数据全空）需要在你现在这个有真实网络的环境里实测才能定论——第一件事就是跑`test_patentsview.py`，把完整的status_code和响应body发给用户。

## 1. 角色与工作方式
- 用户邱正阳，CUHK学生（学号1155028090），GECC4130 4人组组长；本项目此前一直由（云端沙盒里的）Claude协助proposal撰写、数据管道设计、进度报告填写；现在切到桌面版是因为云端沙盒连不了OpenAlex/PatentsView等真实外网，需要本地真实网络+真实key才能实测/采集。
- 沟通偏好：中文，结论先行，技术debug类内容直接给代码块和具体命令，不要重复讲已经讨论过、用户明确表示跳过的话题（见下方密钥部分）。
- 项目铁律（R1-R7"零虚构"）：所有数据必须真实、可溯源，不用估算或AI生成的数值顶替；每个产出文件理论上都要配manifest（源URL、查询语句、时间戳、执行人、SHA256）。
- 协作边界：不要替用户对"密钥要不要轮换"这件事下结论或催促——用户本轮已明确表示这次不处理，如实记录状态即可，不要重提说教。

## 2. 当前位置
| 维度 | 状态 |
|---|---|
| 阶段 | Phase 2 API采集调试中；组员賈贇已跑出一版`raw.zip`（9个CSV），发现3类数据异常 |
| 卡点 | `patentsview_us_patents_by_year.csv` 22行全空，根因有两个假设，需要真实网络+真实key实测才能定论 |
| 下一刀 | 用这个环境跑`scripts/test_patentsview.py`，把打印出来的`status_code`和`body`原文发回给用户 |

## 3. 锁定基线 / Ground Truth
以下均已在本轮核实（读config原文 / 官方文档 / 本地代码复现），不是凭印象，可直接引用：

- **`config/boundary_rules_v1.yaml`**：ai确认`subfields/1702`，biomed确认`fields/27`+`fields/13`（均`confirmed_2026-08-19`）——文件路径：`data_repo/config/boundary_rules_v1.yaml`。
- **`config/institutions.csv`**：11所机构`confirmed`且有ROR（港大/中大/科大/理大/城大/浸大/教大/岭大/应科院/新加坡国立/南洋理工）。
- **OpenAlex API（已查官方文档 help.openalex.org / developers.openalex.org 核实）**：
  - field与subfield是两套不重叠编号体系：field两位数（如27=Medicine，13=Biochemistry, Genetics and Molecular Biology），subfield必为4位数（如1702=Artificial Intelligence，2705=Cardiology）。不存在"subfield 27"这种东西。
  - `group_by`返回的分组条数和`per_page`共用同一个"每页条数"上限（默认最多200/页），`group_by`不支持`page`翻页、只能用`cursor`。
  - `publication_year`字段不支持`2015-2025`这种连字符区间写法，要用`>`/`<`不等式或`from_publication_date`/`to_publication_date`。
- **PatentsView API（已查search.patentsview.org官方文档核实）**：正确endpoint是`https://search.patentsview.org/api/v1/patent/`，正确字段是`assignees.assignee_country`（不是`assignees.country`）、`patent_date`，成功响应应含`total_hits`字段。
- **raw.zip实测现状**（组员賈贇上传，`raw.zip`解压后9个CSV，已逐一比对）：
  - `openalex_papers_by_quarter.csv`：ai、fintech_kw两列正常（240行里160行有真实波动）；**biomed一列80行全部是0**（已确认是bug导致，不是真的没论文）。
  - `openalex_share_denominators_by_year.csv`：应有2产业×2口径×11年=44行，**实际只有2行**（仅`ai/world/2025`、`ai/apac/2025`）。
  - `patentsview_us_patents_by_year.csv`：22行`count`列**全部为空**；且这份文件的表头（`assignee_country`）和賈贇上传的`collect_all.py`里`save_rows`声明的表头（`country`）对不上，说明产出这份csv的脚本版本和现在这份不是同一份。
  - `openalex_universities_works_2015_2025.csv`：**【已更正】不是来源不明的文件**——上一轮误判了，实际上它是2026-08-14 Claude在沙箱里用WebFetch为Gate 1可行性验证跑的七校pilot快照，manifest当时就在（`data_repo/manifests/`里，只是排查时没想到去比对canonical仓库自己的manifests，只查了同学raw.zip里带没带），而且manifest原文就写明"R4待组员本地复跑复核"。跟`openalex_institutions_counts.csv`（同学用collect_all.py正式跑出的11校长表）比对，77组机构×年份里76组对不上、都是pilot这份更低（差0.1%–2%，年份越新差越大）——原因就是两次采集之间OpenAlex索引又回填了新数据，不是bug也不是假数据。已经把这份pilot文件挪到`raw/_archive/`并改名标注`_superseded`，避免继续跟正式数据混在一起；`openalex_institutions_counts.csv`才是应该采信的版本，但它还没被正式并入`data_repo/raw/`（目前只在同学的raw.zip里），这个并入动作还没做。
  - `github_new_users_by_year.csv`、`clinicaltrials_by_year.csv`、`openalex_institutions_counts.csv`：行数齐、数值有真实波动，看起来是干净的。
  - `raw.zip`里**没有带`manifests/`文件夹**，没法核对R2要求的溯源信息。

## 4. 进度盘点
- ✅ 已完成（锁定，勿重做）：
  - `industry_filter()`的field/subfield混淆bug已修复（`data_repo/scripts/collect_all.py`），本地验证过：ai仍是`primary_topic.subfield.id:1702`，biomed现在正确变成`primary_topic.field.id:27|13`。
  - `mod_shares()`的两处bug已修复：区间语法改成`publication_year:>2014,publication_year:<2026`，`per_page`从1改成200。已跑过`python collect_all.py plan`干跑验证无报错。
  - `scripts/test_patentsview.py`已写好（读环境变量`PATENTSVIEW_KEY`，不含任何真实key），用于最小复现patentsview问题：直接打印原始`status_code`+响应body，不像`collect_all.py`里`.get("total_hits","")`那样悄悄吞掉异常。
  - OpenAlex和PatentsView的相关API字段/参数语义已查官方文档核实（见上方Ground Truth），不是凭记忆。
- ⏳ 进行中 / 等外部输入：
  - patentsview根因还差一步实测：两个假设——①`PATENTSVIEW_KEY`是USPTO ODP迁移（2026-03-20生效）前注册的旧版key，新接口认不出但不报错；②请求是`requests.get(url, params={...})`把JSON塞进query string发的，如果接口实际要POST+body可能被当空查询处理。需要真实响应内容才能定论。
  - `openalex_universities_works_2015_2025.csv`来源不明，需要问賈贇这份到底是用什么脚本/方法跑出来的。
- 🔴 待办（按优先级）：
  1. 跑`test_patentsview.py`（PowerShell: `$env:PATENTSVIEW_KEY="他的key"; python test_patentsview.py`），把`status_code`+body原文发给用户。
  2. 确认patentsview根因后，针对性修`collect_all.py`里的`mod_patentsview()`。
  3. 重跑`papers`（只有biomed会变）、`shares`（全部44行）、`patentsview`三个模块。
  4. 问清楚`openalex_universities_works_2015_2025.csv`的来源，没有manifest之前不能进`clean/`。
  5. 【密钥泄露，用户本轮明确表示这次不处理，如实记录，不要重提】：賈贇上传的`collect_all.py`里硬编码了真实OpenAlex key、GitHub classic PAT（`ghp_`开头，注释写"90 Days pass with all access"，权限很大）、PatentsView key、ORCID client id+secret，以及賈贇本人真实姓名和学号邮箱。上一轮已建议立即撤销/轮换（尤其GitHub token）、确认脚本是否被commit/push过。**用户当时的原话是"密钥信息方面不用考虑"**——这是用户的选择，不代表已处理，不要假设已解决，也不要在用户没主动问起时重新提起。

## 5. 待决策项 + 卡点 + 我的判断
| 决策项 | 卡在哪 | 我的判断 | 需要用户 |
|---|---|---|---|
| patentsview根因 | 需要真实key+真实网络跑一次`test_patentsview.py` | 大概率是key类型不对（ODP新旧key分离）或请求方式（GET vs POST）问题，具体待响应内容 | 跑脚本，把输出发回来 |
同学raw.zip里的好文件还没并入仓库 | `openalex_institutions_counts.csv`等賈贇跑出来的干净文件目前只在他发来的raw.zip里，`data_repo/raw/`还是旧的pilot快照 | 需要正式并入`raw/`并补manifest（賈贇的raw.zip没带manifest，得让他把采集时的源URL/时间戳补上，或由你们重新走一遍manifest.py） | 决定谁来做这个并入动作 |
| 密钥是否/何时轮换 | 用户本轮明确跳过 | 不替用户下结论，如实挂起，等用户主动提起 | 用户自行决定 |

## 6. 关键教训 / 反模式（别重蹈）
- OpenAlex的field级ID和subfield级ID是两套不重叠的编号体系（field两位数，subfield四位数）——千万不要不区分来源就统一塞进同一个filter key。
- OpenAlex的`group_by`分组条数和`per_page`共用同一个"每页条数"上限——想要拿到多年/多组数据，`per_page`不能设成1。
- OpenAlex的数值区间过滤不支持`2015-2025`这种连字符写法，要用`>`/`<`不等式或`from_publication_date`/`to_publication_date`。
- 判断一份数据文件能不能信，不能只看"有没有产出文件"，要核对：①表头是不是和产出它的脚本版本一致，②行数是否等于理论预期（产业数×城市数×季度/年数），③跟同口径的另一份文件数值是否一致。这轮能揪出`openalex_universities_works_2015_2025.csv`和patentsview文件的问题，全靠这套核对法，不是凭感觉。
- 遇到`.get(key, 默认值)`这种"安静吞掉异常"的写法要格外小心：它会把"接口返回异常/字段缺失"伪装成"接口正常返回了空值"，脚本看起来跑完了、文件也写出来了，实际数据是坏的。下次调试类似"结果全是0/空"的问题，优先把原始response整个打印出来看，不要只看最终取出来的那个字段。

## 7. 新Session启动方式
把这份文档 + `collect_all.py`（已修复版）+ `test_patentsview.py` 一起粘贴/上传到新对话，开场白：

```
继续GECC4130 FYP的Phase 2数据采集调试。已附交接文档v1 + 修复后的collect_all.py + test_patentsview.py。
当前卡在patentsview根因未定论，需要你在这个有真实网络的环境里跑test_patentsview.py实测。
今天想做：跑通test_patentsview.py，把完整status_code和body给我看；如果能定位到根因，顺手把collect_all.py里mod_patentsview()也一起修了。
读完后先一句话告诉我：你接到哪一步 + 建议我立刻做什么。
我的偏好：中文，结论先行，技术细节直接给代码块和具体命令，不要重复讨论密钥轮换的事（我知道，这次不处理）。
```

## 8. 对新Claude的工作提示
- 绝不要做的事：不要凭记忆断言OpenAlex/PatentsView的API细节（本文档已核实的内容可以直接引用，遇到新的类似问题要重新查证官方文档）；不要在没有manifest之前把`openalex_universities_works_2015_2025.csv`当真数据用；不要替用户判定密钥问题"已解决"，也不要没被问起就重新说教。
- 历史教训：上一个Claude一开始差点直接采信"票面上count=0就是真实结果"，是靠交叉核对config文件+官方文档才揪出真正的bug——处理这个项目的数据异常，默认先怀疑"这是bug"，而不是"这就是真实的0"。

## 9. 缺口清单
- 賈贇raw.zip里的`manifests/`文件夹：他发过来的zip没带，R2要求的溯源信息（源URL/时间戳/哈希）目前缺失，需要问他要，或者重新手动补。
- 賈贇good的输出（`openalex_institutions_counts.csv`、`github_new_users_by_year.csv`、`clinicaltrials_by_year.csv`、ai/fintech_kw论文数据）还没正式并入`data_repo/raw/`——这个动作还没做，需要决定由谁来做。
- 密钥是否已经/准备轮换：未知，用户本轮主动跳过了这个话题。
- 賈贇本人的操作系统/运行环境：未知（这次发现PowerShell语法问题是因为用户自己的机器是Windows；賈贇如果是Mac/Linux，之前给的bash版本命令对他是对的）。

## 10. 本文档v1的一处更正
第3节里原本说`openalex_universities_works_2015_2025.csv`"来源不明、没有manifest"——这个判断错了，是上一个Claude排查时只查了賈贇raw.zip里带没带manifest，没想到回头查canonical仓库自己的`manifests/`文件夹，其实一直都在。已在第3/5节更正并把该文件归档，写在这里是提醒新Claude：判断"有没有manifest"之类的溯源问题时，要连canonical仓库自己的记录一起查，不能只看对方发过来的那一份。
