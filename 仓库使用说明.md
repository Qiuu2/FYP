# 仓库怎么用（第一次看请读完，5 分钟）

我们的数据和文档从现在起全部放在一个公开仓库里：

## 👉 https://github.com/Qiuu2/FYP

以后**不再用微信发压缩包**。所有脚本、配置、文档、任务单都从这里取。

---

## 一、第一次：把仓库拉到自己电脑

**方式 A（推荐，以后更新只要一条命令）**

先装 git：https://git-scm.com/download/win　一路下一步即可。

然后打开 PowerShell：

```powershell
cd C:\          # 或你想放的任何地方
git clone https://github.com/Qiuu2/FYP.git
cd FYP
```

**方式 B（不想装 git）**

网页上点绿色 **Code** 按钮 → **Download ZIP** → 解压。
缺点是每次更新要重新下载整个包。

---

## 二、每次开工前，先拉最新的

```powershell
cd <你放 FYP 的位置>
git pull
```

**这一步很重要。** 不拉就干活，可能用的是旧脚本、旧口径，白做。

---

## 三、里面有什么

| 目录 | 内容 |
|---|---|
| `tasks/任务单/` | **你的任务单在这里**，按姓名和周次命名 |
| `tasks/任务台账.md` | 谁在做什么、截止什么时候、现在什么状态 |
| `tasks/回传/` | 已交的东西原样留档 |
| `scripts/` | 全部脚本 |
| `config/` | 边界规则、公司名单、判定表 |
| `docs/` | 所有口径决定与分析文档 |
| `raw/` `clean/` | 数据 |

---

## 四、交东西

**暂时还是发给组长**（微信/邮件都行），组长代为提交。

想直接提交的话跟组长说一声，加你为 collaborator，之后：

```powershell
git pull                              # 先拉最新
# ...干活...
git add -A
git commit -m "一句话说明改了什么"
git push
```

---

## 五、三条规矩

**① 密钥绝对不能进仓库。**
仓库是**公开的**，全世界可见。API key 一旦提交，删文件也没用，得重写整个仓库历史。
你自己的 `.keys.env` 放在本地，**永远不要**把它加进 git、不要贴进聊天。

**② 回传的东西原样交，不要自己修饰。**
数字不好看、跟预期不符、甚至跟组长说的矛盾——都照实交。
发现问题反而是有价值的（賈贇上次就纠正了文档里一处事实错误）。

**③ 判定类任务只填事实，不填结论。**
比如查公司资料，就记「登记主营活动写的是 X，来源 URL 是 Y」，
不要直接写「所以它没有研发」。结论由组长综合各条证据后下。

---

## 六、为什么公开

仓库里保留了我们推翻自己结论的记录（比如 `docs/_作废_...`、`效度关结果_修订`），
也保留了任务台账里的逾期和返工记录。

这不是为了追责——**是因为完整的审计轨迹比一个干净得可疑的仓库更可信。**
提案 §5 承诺交付「可复现代码管道」，这就是兑现。

台账里有一栏专门记组长自己犯的错（目前 5 条），一视同仁。
